name: Monitor - Atualizar HTML e Enviar Alertas

on:
  schedule:
    # Executa a cada 10 minutos durante horário do mercado
    # 9h-19h BRT = 12h-22h UTC
    # Minutos: 0, 10, 20, 30, 40, 50
    - cron: "0,10,20,30,40,50 12-22 * * *"
  
  # Permite execução manual
  workflow_dispatch:
  
  # Ao fazer push no monitor.py
  push:
    branches: [main]
    paths: [monitor.py]

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run monitor
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
        run: python monitor.py
      
      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add relatorio_monitor.html data/current-analysis.json
          if git diff --cached --quiet; then
            echo "✅ Sem mudanças para commitar"
          else
            git commit -m "🔄 atualização automática - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi
